CS 405 Original Google Test Suite

This archive preserves the original submitted C++ source file that supported Project Two.

The file contains a Google Test fixture and 15 vector-focused tests. It was created for a Visual Studio project that uses precompiled headers, so it is preserved as submitted rather than forced into the portable CMake build.

The repository-level test suite contains a portable regression version of the same 15 behaviors.
