# Coding Archives

`CS405_Original_GoogleTest_Suite.zip` preserves the original submitted vector test source.

`Ransom_Note_That_Lied_Source.zip` contains the standalone CMake application, tests, and sample evidence files.

The same application source remains visible at the repository root so a reviewer can inspect the code and GitHub Actions can build it without unpacking an archive.
